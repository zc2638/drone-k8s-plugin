apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}
  namespace: {{ app_namespace }}
  labels:
    app: {{ app_name }}
spec:
  ports:
    - nodePort: 30090
      port: 9090
  selector:
    app: {{ app_name }}
  type: NodePort
